model User {
  id       String   @id @default(cuid())
  name     String
  email    String   @unique
  password String
}

model Product {
  id       String   @id @default(cuid())
  name     String
  price    Float
  description String
}